@model IEnumerable<BarbaraDoces.Models.NovidadeViewModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TituloNovidade)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DescNovidade)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DtNovidade)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TituloNovidade)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DescNovidade)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DtNovidade)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdNovidade }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdNovidade }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdNovidade })
        </td>
    </tr>
}

</table>
